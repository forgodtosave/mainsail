# only Comment
# only blank lines
# Comment first line with configuration underneath
# blank line with configuration underneath
# Comment line between config blocks / parameters 
# blank line between config blocks / parameters 
# Comment after a [Block], a parameter, and after a value

# only import
# import between config blocks
# import of a file in same directory
# import of with parth starting with "/", "./", "../", "~/"
# import of with a relative path
# import with wrong syntax

# only empty config block
# only config block
# block type with special characters 
# block type with non valid characters 
# block type and identifier with special characters
# block type and identifier with non valid characters

# parameter at non valid positions
# parameter without following ":" + value/valueBlock
# parameter-name with special characters
# parameter-name with non valid characters
# parameter with normal value
# parameter with valueBlock
# parameter ending on "gcode" + value
# parameter ending on "gcode" + valueBlock

Pin | pins | VirtualPin  | Cords | Number | String | Boolean | Path | FilePath | Resolution | Ip

# normal Pin
# inverted Pin
# pullup/low Pin
# combination of iverted and pullup/low pin
# wrong combination of iverted and pullup/low pin
# lowercase Pin
# pins
# virtual pin 
# virtual pin with non valid characters

# int
# float
# wrong number
# cords 
# mixing float/int in cords

# normal string
# string with whitespaces
# string with special characters
# url, ... as string
# string with non valid characters like "#" and "[]"

# boolean 

# Path 

# Resolution 
# non valid Resolution

# Ipv4
# Ipv4 with prot
# ipv4 with subnetsize
# Ipv4 with wrong syntax 


